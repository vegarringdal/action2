name: Shared workflow

on:
  workflow_call:
    inputs:
      config:
        required: true
        type: string
    secrets:
      TEST1:
        required: true


jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - name: Init - Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Echo response
        run: echo Someone called for me? ${{inputs.config}} ${{contains(secrets.TEST1, "TEST")}}